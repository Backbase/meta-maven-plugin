images:
  image:
  - registry: experimental
    image: ${project.artifactId}
    tag: ${project.version}
    hello: world
  - registry: staging
    image: ${project.artifactId}
    tag: ${project.version}-special
    hello: dolly
    suffix: special
array:
- item1: value1
- item2: value2
- item3
map:
  key1: value1
  key2: value2
